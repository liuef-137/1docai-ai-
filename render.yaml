services:
  - type: web
    name: docai
    runtime: python
    rootDir: .
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn "wsgi:application" --bind 0.0.0.0:$PORT --timeout 180 --graceful-timeout 30 --keep-alive 5
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.6"
      - key: SECRET_KEY
        generateValue: true
      - key: DEEPSEEK_API_KEY
        sync: false
      - key: DEEPSEEK_BASE_URL
        value: https://api.deepseek.com
      - key: DEEPSEEK_MODEL
        value: deepseek-v4-flash
      - key: ADMIN_PASSWORD
        sync: false
    healthCheckPath: /
