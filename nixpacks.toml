[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn wsgi:application --bind 0.0.0.0:$PORT --config gunicorn.conf.py --timeout 45 --graceful-timeout 10 --keep-alive 5"
